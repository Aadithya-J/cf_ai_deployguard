{
  "extends": "agents/tsconfig",
  "exclude": ["demo-worker", "node_modules", "dist"],
  "compilerOptions": {
    // Runtime types come from `wrangler types` (env.d.ts), not the
    // @cloudflare/workers-types package.
    "types": ["node", "vite/client"]
  }
}
